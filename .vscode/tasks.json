{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "docker_run",
            "type": "shell",
            "command": "docker run --rm -d --name php-apache -p 80:80 -v ${workspaceFolder}:/var/www/html vmzcloud/php-apache:8.0.27-ext",
        }
    ]
}